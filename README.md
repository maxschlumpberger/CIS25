Week 8:

This code starts with the inclusions neccessary for the program. The program then creates a class "BankAccount", and the private section of this class which holds a string var to store the account name and a double var to store the account balance. It also has a void function that opens the file to log transaction type, amount, transaction description, and the new balance. Once it is done logging, it closes the file. Next, in the public section, we have a constructor that initializes the balance, sets up the account name, and logs the account creation. It creates a method of purchasing an item, with an if statement that covers the possiblity of the user purchasing faulty amounts like ones that are over the current balance or equal to or below 0. It logs the purchases and purchase details. It also creates a method to deposit money into the account, which also includes an if statement in case the user enters in an amount equal to or below 0. It logs the deposit as well, including the amount deposited and type of deposit. Next, the code has a way for the user to check their balance, which will print the balance. Lastly, in the main, I create a bank account object, with a name for the account owner and an intial balance. Then, I test the deposit method, purchase method, and the checking balance method. Once that is done, it ends with returning 0.

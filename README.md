Module 6:
This program creates an item class that stores product info, to save/load from a file. We begin by creating a class, constructors, and a function that will write the item data. We also create a function that will read the file, then we create getters and setters to acces the data, followed by the main() function where we test everything out by creating and setting the item up, displaying the item, saving it to file, and loading it.

Module 11:
This code starts with the necessary libaries for string handling and input/output as well as avoiding writing std:: in front of cout and cin. It follows with creating a class, declaring public attributes like the id, name, and price. Then we create a constructor that runs automatically when the object is created, taking 3 parameters in id, name, and price. It then assigns those to the object's attributes. Next we create a destructor that runs automatically when object is destroyed. Next we have a void function that prints all three attributes of name, id and price. Lastly, we have a the main() of the code where we test everything out, creating an object, passing intitial values, and then returning 0 when everything runs correctly.

In this module 7 assignment, the code begins with all the necessary library inclusions. Then it creates the struct item, giving it two members in name and id. Then it defines a binary search function to search through an array of structs, so that it can quickly narrow down the list by halfing the possible ID's each iteration. Then in the main function, the code defines a const int for the size of the array so that it the size will be unchanged, then declares a pointer, and dynamically allocates memory for 100 structs. Then I created the sample names for all the items, sorted alphabetically. Next I created a for loop that would iterate through every item in the inventory, giving them an id, starting with 10 all the way to 109, and assigning each item a name from the sample names. Then the code console outputs the list that it populated, followed by asking the user to input an id. Once the user inputs the id, the code uses the binary search to match the id to one of the items, prints the item name and id if it exists, prints that it is not found if it doesn't exist, and then deallocates the memory.

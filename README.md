Week 7 Part 1 code:
My code for part 1 is as instructed, a simple menu. The code starts by defining an int var "userChoice" and setting it to 0. This var will hold the user's input for what they want to do in the menu. We also declare a bool var named "exit" and set it to false. This will be used for a do-while loop in order to eventually give the user the option to exit the menu. We console output the available options in adding an item, viewing the display, and exiting the menu. Then we cin the user's input. Next we use an if statement to go through the possible inputs the user made, beginning with 1, which would lead to the text "Your item was added! Press 1 to add another item!" being displayed. If the user input 2, they would be told their items are being displayed. Next, if the user input 3, the text "Exiting..." would display, and the bool var "exit" will be set to true, causing the user to exit the loop and the program. Lastly, we have an else statement that holds the message that the user's input is invalid and to please try again, with clarification on what is a good input. This will trigger if the user inputs something that is not an available input. Finally, we return 0; to conclude the program.


Part 2 code:
In this part of the assignment, we are tasked with debugging some faulty code. The first problems with the given code are that there are no commas separating each float var from eachother. We fix that by adding a comma after each float declaration. The next issue is that when we cin the user's three numbers, the floats are separated by ">" instead of ">>". Fixing that issue will help us get all of the user's three numbers. Moving on, the instructions want us to use a nested if statement instead of the three separated if statements in the faulty code. The faulty code also is missing a parantheses after the first if statement criteria is declared. There are also no curly brackets containing the cout afterwards. In the next if statement, there is no closing curly bracket after the cout. And in the third if statement there is no beginning curly bracket to hold the cout. In the fixed code, we make a nested if statement that doesn't need the && in the criteria, since we logically progress from each scenario with a nested if. If n1 is greater or equal to n2, we then check if n1 is greater or equal to n3. If it is, then n1 is the largest number, else n3 is the largest. We also have an "else if" in the case that n2 is greater than n1, which then leads to us checking if n2 is also greater than n3. If it is, n2 is declared the greatest, else it is n3 that is the greatest. We conclude the nested if statement and the program with "return 0;".
